**行为修正**：贴边与居中改用「整块屏幕」的几何范围计算，不再用
`availableGeometry()`（那是扣掉任务栏后的区域），所以任务栏不会再把位置顶偏。

- 居中 = 屏幕真正的中心（本机 1920×1080 上从 y=498 回到 y=528）
- 贴边 = 屏幕真正的四角（本机左下角的 y 从 996 回到 1056）
- 窗口本身是置顶的，贴到下方时会盖在任务栏上面，这正是想要的效果
- 自由位置（拖动后的坐标）同样按整块屏幕判断是否还在屏内

## 验证

冻结版实测：配置成「左下、边距 0」，截图在左下角（压在任务栏上）抓到 68 个
准星红像素，而旧的任务栏避让位置上 0 个红像素 —— 确认贴的是整块屏幕的角。

## 下载

`screen-overlay-v1.0.2-win64.zip`，解压后双击 `screen-overlay.exe`。
